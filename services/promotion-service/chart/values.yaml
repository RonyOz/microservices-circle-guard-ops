replicaCount: 1

podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "8086"
  prometheus.io/path: /actuator/prometheus

image:
  repository: ""  # set at deploy time by GHA: --set image.repository=<registry>/circleguard
  pullPolicy: IfNotPresent
  tag: ""  # Overridden by pipeline: --set image.tag=<service>-sha-<commit>

service:
  type: ClusterIP
  port: 8086

resources:
  requests:
    cpu: 150m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 768Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80

startupProbe:
  httpGet:
    path: /actuator/health/liveness
    port: 8086
  initialDelaySeconds: 10
  periodSeconds: 10
  failureThreshold: 12

livenessProbe:
  httpGet:
    path: /actuator/health/liveness
    port: 8086
  initialDelaySeconds: 0
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /actuator/health/readiness
    port: 8086
  initialDelaySeconds: 20
  periodSeconds: 5

env:
  JAVA_TOOL_OPTIONS: "-Xmx384m -Xms256m -XX:+UseContainerSupport"
  DB_HOST: "postgres"
  DB_NAME: "circleguard_promotion"
  REDIS_HOST: "redis-master"
  KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"
  NEO4J_URI: "bolt://neo4j:7687"

envFrom:
  - secretRef:
      name: promotion-service-secret
      # Keys expected: DB_USERNAME, DB_PASSWORD, NEO4J_USERNAME, NEO4J_PASSWORD
